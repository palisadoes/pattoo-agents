[Unit]
Description=pattoo-os-snmpd daemon
Wants=network.target
After=network.target

[Service]
Environment="PATTOO_CONFIGDIR=x"
Environment="BIN_DIR=x"
ExecStart=$BIN_DIR/pattoo-os-snmpd --start
ExecStop=$BIN_DIR/pattoo-os-snmpd --stop
ExecReload=$BIN_DIR/pattoo-os-snmpd --restart

[Install]
WantedBy=multi-user.target
